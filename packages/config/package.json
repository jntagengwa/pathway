{
  "name": "@pathway/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo config-build",
    "lint": "eslint .",
    "typecheck": "tsc -v",
    "test": "echo config-test"
  }
}
