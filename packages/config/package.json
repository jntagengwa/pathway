{
  "name": "@pathway/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo config-build",
    "lint": "eslint \"src/**/*.{ts,tsx,js}\" --no-error-on-unmatched-pattern",
    "typecheck": "tsc -v",
    "test": "echo config-test"
  }
}
